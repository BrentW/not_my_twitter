<% @posts.each do |post| %>
<div class="post">
<h2><%= link_to post.title, post %></h2>
<p><%= truncate(post.body, length: 100) %></p>
<p><%= post.created_at.strftime("%B %-d, %Y") %></p>
</div>
<% end %>